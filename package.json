{
  "name": "ministore",
  "version": "1.0.0",
  "description": "for electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build_mac": "electron-packager . miniStores --platform=darwin --arch=x64 --out=dist/ --overwrite --ignore=./node_modules/* --ignore=./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chris chiang",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.8.4"
  },
  "devDependencies": {
    "electron-packager": "^11.1.0"
  }
}
